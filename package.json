{
  "name": "ai-fde-fieldbook",
  "version": "1.0.0",
  "private": true,
  "description": "A practical, mastery-gated fieldbook for AI Forward Deployed Engineering.",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "vitepress dev docs --host 127.0.0.1",
    "build": "npm run check && vitepress build docs",
    "preview": "vitepress preview docs --host 127.0.0.1",
    "check": "node scripts/check-content.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": { "vitepress": "1.6.4", "@lucide/vue": "1.41.0" },
  "overrides": { "vite": "6.4.3" }
}
